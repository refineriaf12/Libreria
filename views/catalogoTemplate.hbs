{{>organisms/navBar}}
<main>
    <section>
        {{#if decision}}
            {{>organisms/listaLibros}}
        {{else}}
            {{>organisms/listaDiscos}}
        {{/if}}
    </section>
</main>